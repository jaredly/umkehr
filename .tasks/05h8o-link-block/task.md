examples/block-rich-text: I want the `link` mark to be able to point to other blocks on the page, like ` #id-href`. maybe it can literally be that. we just need to make sure that the blocks have `id` set. To make a block link, you block-select a block, copy it (cmd-c), and then select some text, and paste. A "block paste over a text selection" should trigger a 'link mark' instead of replacing the text.
