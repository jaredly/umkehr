examples/block-rich-text: I want a block type that is 'movable block'. This would take it out of the normal linear rendering, and render it with absolute positioning. The metadata would contain the top/left coordinates.
This block type would only be respected by children of the root, or by direct children of `slide` blocks
