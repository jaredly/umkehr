if you click-drag across multiple blocks, we should switch to "block selection mode" where we don't have cursors anymore, we're selecting blocks as a whole. It should be possible to cmd+c in this mode, and it should copy the blocks and any associated marks/comments/footnotes in a format that could be pasted back into the editor. We should also support drag-to-reordering of multiple selected blocks.
