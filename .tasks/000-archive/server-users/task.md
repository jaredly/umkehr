I want the examples/react-crdt/src/lib/server architecture to support users. We don't need passwords or anything, as this is just an example. The user sets their nickname (or picks from a list of known nicknames providede by the server) upon first opening the page. The server responds with a user-id, which should be incorporated into the hlc's `node`, although there should also be a unique session id element as well. This user-id is persisted locally, and the user should be able to 'log out', clearing their user-id, enabling them 'log in' again.
