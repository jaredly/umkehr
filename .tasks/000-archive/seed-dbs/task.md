I would like to be able to run a script which will generate a server sqlite database with a bunch of different test documents in it, demonstrating different cases we would want to evaluate (for example a ton of changes, or a ton of todo items, complex branch topology, etc.). And then I want the web side to allow you to select which document (by id in a query param) you want to load. There should probably also be a dropdown in the UI for switching between them. Once that is done, we can
When running the react-crdt-server with `bun dev`, I should be able to select between the "test db" and a "normal usage db".
