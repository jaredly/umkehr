One thing that's been bothering me is the `CRDTLocalHistory`'s `undoStack` and `redoStack` being "local commands", duplicating/denormalizing from the CRDTUpdates. What are my options for removing this duplication, and relying only on the CRDTUpdate type? One thought is to maybe add a piece of metadata onto client's CRDTUpdates, indicating whether it's an "undo" of a previous update. given that each CRDTUpdate has an HLC that we can use to know whether it was authored by the current user session, maybe that would be enough?
