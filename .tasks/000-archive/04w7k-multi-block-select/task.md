examples/block-rich-text: When we're in multi-select mode, we can shift-arrow a selection over multiple blocks. This is cool. However, in non-multi-select mode, we cannot. Also, pressing "backspace" when selecting across a block boundary should join those two blocks. It currently does not. I've recently implemented several selection-related fixes, and it might be worth going over all of the selection-handling code wholistically to ensure a good architecture.
