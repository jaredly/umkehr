I'd like a comprehensive test suite for the RGA+blocks CRDT in src/block-crdt. It should fully exercise all the CRDT operations, including split, merge, and concurrent edit scenarios. Where possible, treat the algorithm as a black box, with the test suite "adding characters" and performing "split / join" operations, and then making assertions about the serialized-text version of the CRDT state.
