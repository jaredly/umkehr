examples/block-rich-text: cell dragging should support dragging out of the table, as normal blocks.
It should also support 'drop targets' that are 'the spaces between rows', which would insert the cells as a new row (possibly with missing columns)
Dragging blocks from outside the table, on the other hand, currently *only* have row drop targets; I'd like there to be cell drop targets (vertical lines between cells) as well.
