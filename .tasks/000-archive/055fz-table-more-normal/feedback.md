
- rows shouldn't wrap. if there are more columns, the cells should be smaller (shrinking to a reasonable minimum), after which the table should scroll horizontally
- rows should be draggable into or out of the table entirely (to/from being normal blocks)
- row headers, when empty, should have the placeholder be the row index, not the normal ellipsis
- let's make the table header be rendered visually as part of the table, not just above it. we can move the table over to the right so that it's more visually consistent with other block types
