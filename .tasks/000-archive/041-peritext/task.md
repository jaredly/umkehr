I'd like an implementation of peritext that we can integrate into umkehr. We probably don't want to directly use the reference implementation (https://github.com/inkandswitch/peritext) because it's pretty integrated with micromerge and prosemirror, but we can use it as a reference. Let's put it in src/peritext. We'll then integrate it with umkehr roughly following the plan in .tasks/richtext.
