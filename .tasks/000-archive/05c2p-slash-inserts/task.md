examples/block-rich-text: let's have / (forward slash) trigger a popover menu, where your focus jumps to a search bar at the top of the popover menu, and you can choose from various options, including (change the block type) and (insert some inline embed). The slash should be actually added to the document, and pressing `escape` should close the popover and let you continue typing. Selecting something from the popover menu should delete (tombstone) the forward slash character.
