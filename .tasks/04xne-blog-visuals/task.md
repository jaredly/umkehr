I've got some ascii-art diagrams in `src/block-crdt/Blog Post.md`, and I'd like high-fidelity SVG versions of them.
Let's add them to `examples/block-rich-text` and have them show instead of the editor if there's a `?demos` query parameter.
